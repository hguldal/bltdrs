@{
 if (!WebSecurity.IsAuthenticated)
     {
        
        Response.StatusCode=401;
        Response.End(); 
     }
    var guid=Guid.NewGuid().ToString();
    Response.Write(guid);
    Response.End();
}

