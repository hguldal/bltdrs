@{
 if (!WebSecurity.IsAuthenticated)
    {
        Response.StatusCode = 401;
        Response.End();
        
    }
 var db=Database.Open("fb1ae");
  db.Execute("DELETE FROM BildirimOkunmasiGereken WHERE UserID=@0",WebSecurity.CurrentUserId);
  db.Close();
  Response.Write("OK");
  Response.End();
}
